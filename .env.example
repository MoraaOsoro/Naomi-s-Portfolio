# Optional — required only to make the contact form actually deliver email.
# Without these, submissions are accepted (visitors see a success state) but
# not sent anywhere. See README.md → "Connecting the contact form".

# API key from https://resend.com
RESEND_API_KEY=

# The inbox that should receive contact-form messages
CONTACT_TO_EMAIL=missmoraaosoro@gmail.com
